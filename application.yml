server:
  port: 3344

spring:
  application:
    name: config-server
  #链接远程配置仓库
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tulipforyou/springcloud-config.git
          basedir: data/
